"""Cross-subsystem helpers.

Reserved for code whose consumers span more than one subsystem
(``calculations/`` and ``workgraphs/``). Helpers shared only among
workgraphs live in ``workgraphs/utils/`` instead; a module's home
encodes its widest consumer.
"""
